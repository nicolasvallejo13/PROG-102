pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'echo build'
        sh 'ls -la'
      }
    }

    stage('unittest') {
      steps {
        sh 'echo tesing'
      }
    }

  }
}